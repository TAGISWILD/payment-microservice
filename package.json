{
  "name": "payment-service",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "build": "echo 'Root build script - this should not run'",
    "heroku-postbuild": "echo '=== Building React Frontend ===' && cd frontend && echo 'Installing dependencies (including dev)...' && npm ci --include=dev && echo 'Building React app...' && npm run build && echo '=== React build completed ==='"
  }
}

